# Derive properties from default application.properties

# Profile is deployment
simplerest.environment=deployment

# MySQL datastore
spring.datasource.url=jdbc:mysql://localhost:3306/simplerest?useSSL=false
spring.datasource.username=appUser
spring.datasource.password=reallyNotSecure

